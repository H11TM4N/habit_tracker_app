class Habit {
  String title;
  String question;
  bool isDone;
  bool isEditing;

  Habit({
    required this.title,
    required this.isDone,
    required this.question,
    required this.isEditing,
  });
}
