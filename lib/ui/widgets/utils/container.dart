import 'package:flutter/material.dart';


Widget kContainer(Widget widget) {
  return Container(
      color: Colors.black38,
      margin: const EdgeInsets.all(3),
      padding: const EdgeInsets.all(8.0),
      child: widget,
    );
}