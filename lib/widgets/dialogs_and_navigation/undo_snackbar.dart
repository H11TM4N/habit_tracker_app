import 'package:flutter/material.dart';

class UndoSnackBar extends StatelessWidget {
  const UndoSnackBar({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}